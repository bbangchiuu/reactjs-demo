import { ReactNode } from "react";



export interface IChildrenReactNodeProps {
    children: ReactNode;
    // any other props that come into the component
}

export interface DefaultParam{
    id : string
}

